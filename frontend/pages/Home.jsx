import React from 'react';
import './styles/Home.css';

{/* TODO: Maybe do the home page, idk though */}

const Home = () => {
  return (
    <div>
      <h1>Home Page</h1>
      {/* CODE HERE IN THE FUTURE */}
      <p>Make 3 lines like a roulette wheel, they kinda spin(move) from one side to another(first to right, second left, third right) 
        when user clicks on the button, it selects the id of the item and starts stopping each line at this id, then gives a link
        to that item or button to reroll</p>
    </div>
  );
};

export default Home;
